---
- name: install and configure puppetserver
  hosts: all

  user: root
  user: root
  roles:
    - puppetmaster


# - lineinfile:
#   - path: /etc/hosts
#    - regexp: 127.0.0.1\s+puppet
#    - line: "127.0.0.1     puppet"

